import { Quote } from '../components/layouts';
import type { SlideDef } from '../deck/types';

const slide: SlideDef = {
  content: <Quote by="Someone worth quoting">A short quote lands harder than a long paragraph.</Quote>,
};

export default slide;
